<UserControl x:Class="ArgesDataCollectionWithWpf.UI.UIWindows.CustomerUserControl.DataModifyAndShowUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ArgesDataCollectionWithWpf.UI.UIWindows.CustomerUserControl"
             mc:Ignorable="d" >
    <UserControl.Resources>
        <local:LabelDataSourceBinding x:Key="labelSourceBingKey"/>

        <local:TextDataSourceBinding x:Key="txtSourceBingKey"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>

        </Grid.RowDefinitions>

        <Label Name="lbl_Index" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20" Content="{Binding  Path=  LabelData}" />
        <TextBlock Name="txt_plcValue" Grid.Row="1"  VerticalAlignment ="Center" HorizontalAlignment="Center" FontSize="20" Text="{Binding Path=TextData}"></TextBlock>

    </Grid>

</UserControl>
