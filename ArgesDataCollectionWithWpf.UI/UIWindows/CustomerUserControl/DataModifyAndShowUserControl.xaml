<UserControl x:Class="ArgesDataCollectionWithWpf.UI.UIWindows.CustomerUserControl.DataModifyAndShowUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ArgesDataCollectionWithWpf.UI.UIWindows.CustomerUserControl"
             mc:Ignorable="d" Height="30" Width="200">
    <UserControl.Resources>
        <local:LabelDataSourceBinding x:Key="labelSourceBingKey"/>

        <local:TextDataSourceBinding x:Key="txtSourceBingKey"/>
    </UserControl.Resources>
    <Grid>
        
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            
        </Grid.ColumnDefinitions>

        <Label Name="lbl_Index" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="10" Content="{Binding  Path=  LabelData}"> </Label>
        <TextBlock Name="txt_plcValue" Grid.Column="1" IsEnabled="false" VerticalAlignment ="Center" HorizontalAlignment="Left" FontSize="10" Text="{Binding Path=TextData}"></TextBlock>
            
    </Grid>
</UserControl>
